name: My First CI/CD Pipeline
on:
  push:
    branches:
      - main
jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      - name: A simple step
        run: echo "Hello, world!"
